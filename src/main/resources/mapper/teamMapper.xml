<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.candm.mapper.TeamMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into tbl_team (team_title, team_url, team_contact_email, team_intro, member_id)
        values(#{teamTitle}, #{teamUrl}, #{teamContactEmail}, #{teamIntro}, #{memberId})
    </insert>
    <select id="selectByTitle" resultType="teamVO">
        select id, team_title, team_url, team_contact_email, team_status, team_intro, member_id
        from tbl_team where team_title = #{teamTitle}
    </select>

    <select id="selectByUrl" resultType="teamVO">
        select id, team_title, team_url, team_contact_email, team_status, team_intro, member_id
        from tbl_team where team_url = #{teamUrl}
    </select>
    <select id="selectAllByMemberId" resultType="teamVO">
        select
            id, team_title, team_url, team_contact_email,
            team_status, team_intro, member_id, created_datetime, updated_datetime
        from tbl_team
        where member_id = #{memberId}
        order by created_datetime desc
    </select>
</mapper>
