<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.candm.mapper.MessageRoomMapper">

    <!-- 방 생성 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into tbl_message_room (created_member_id, invited_member_id)
        values (#{senderId}, #{receiverId})
    </insert>

    <!-- 방 조회 -->
    <select id="selectByCreatedMemberIdAndInvitedMemberId">
        select id from tbl_message_room
        where created_member_id = #{senderId} and invited_member_id = #{receiverId}
    </select>

    <!--목록 조회-->
    <select id="selectAllListByMemberId" resultType="MessageRoomDTO">
        select m.member_name,
               msg.message_room_id,
               msg.message_content,
               msg.notification_datetime,
               msg.member_id as invited_member_id
        from tbl_member m
        join (
            select msg2.message_room_id,
                   msg1.message_content,
                   msg1.notification_datetime,
                   case
                       when msg1.receiver_id = #{memberId} then msg1.sender_id
                       else msg1.receiver_id
                   end as member_id
            from tbl_message msg1
            join (
                select message_room_id, max(id) id
                from tbl_message
                where receiver_id = #{memberId} or sender_id = #{memberId}
                group by message_room_id
            ) msg2
            on msg1.id = msg2.id
        ) msg
        on m.id = msg.member_id
    </select>

</mapper>
