drop table tbl_team_category_current_level_team;
create table tbl_team_category_current_level_team(
    id bigint unsigned auto_increment primary key,
    category_current_level_name varchar(255) not null
);